{
  "name": "strmr-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-tenant AI avatar livestream platform built on LiveKit, Hume EVI, Beyond Presence, and Stripe AgentKit.",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "3.3.3",
    "turbo": "2.1.2",
    "typescript": "5.6.3"
  }
}
